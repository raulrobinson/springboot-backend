# ---------------------------------
# --- APP General Configuration ---
# ---------------------------------
springdoc:
  server:
    address: localhost
    port: 8082
server:
  port: 8082

# --------------------------------
# --- Database (secret-cnx-db) ---
# --------------------------------
spring:
  jpa:
    open-in-view: false
    show-sql: false
    hibernate:
      ddl-auto: none
  datasource:
    hikari:
      maximum-pool-size: 1
      minimum-idle: 1
      maxLifetime: 600000
